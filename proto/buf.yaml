version: v2
modules:
  - path: .
lint:
  use:
    - STANDARD
breaking:
  use:
    - FILE
  ignore:
    # One-time: dataplane/v1/placeholder.proto was deleted and replaced by
    # the real Phase-01 internal contract in dataplane.proto. Removed in the
    # next proto-touching commit once this comparison is no longer live.
    - dataplane/v1/placeholder.proto
