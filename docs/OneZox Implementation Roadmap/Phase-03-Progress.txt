================================================================================
PHASE-03 — DATA PLANE FAST PATH — PROGRESS / FINDINGS RECORD
================================================================================
This file is NOT a step-by-step changelog of Phase-03 code changes — git log
and each commit message are the record for that (every Phase-03 commit is a
verified, independently-testable checkpoint per CLAUDE.md). This file exists
for things git history doesn't capture on its own: deviations from expected
behavior, process gaps, and findings that future sessions (or a fresh
engineer) need to know about before they re-discover them the hard way.
Entries are dated and point-in-time. Matches the same convention
Phase-01-Progress.txt and Phase-02-Progress.txt established.

--------------------------------------------------------------------------------
2026-08-04 — Steps A1-A8: real token-usage propagation + ProviderHealth,
completing two Phase-02-declared-but-deferred obligations at their first
real consumer
--------------------------------------------------------------------------------
CONTEXT: Phase-03's plan reached Step A (contracts) and found provider-gateway
had no mechanism to carry real token usage at all — Delta had no usage field,
and none of the three real adapters even parsed a usage payload out of their
provider's response (OpenAI: stream_options.include_usage was never set;
Anthropic: message_start was discarded entirely; Google: usageMetadata was
never read). This lines up with Phase-01-Complete.txt and Phase-02-Complete.txt
both already naming real metering as "Phase-03's job" — not a P02 defect, an
expected gap closed at its first real consumer. Separately, ProviderHealth
(declared in Phase-02.txt's own APIS CREATED section) was still Unimplemented
for the same reason: no consumer existed before Phase-03's scheduler.

BOUNDARY HELD (explicit user instruction, worth recording because it shaped
every line of this work): provider-gateway CARRIES and REPORTS usage; it
never ACTS on it. No token-based rate limiting (that's Dependencies.txt F2,
P13), no cost-routing (P12-P14's learned router), no analytics (P13). Confirmed
at the end of Steps A1-A7: no new code path reads InputTokens/OutputTokens for
any decision — only reads and forwards.

A1: provider.proto's Delta gains optional int32 input_tokens/output_tokens.
Presence IS the signal (proto3 optional) — set means a real number, unset
means "not reported," never coerced to zero anywhere downstream. buf lint and
buf breaking both pass clean (purely additive).

A2: internal adapters.Delta mirrors the same two fields; relay.go carries them
through unconditionally when set. CRITICAL design point, per explicit
instruction to check the unhappy path before building it: "final delta only
on the happy path" would create a billing hole — a mid-stream provider
failure after real tokens were already generated would otherwise report
nothing, and a caller could easily (wrongly) read that as zero. Fixed by
having main.go's Invoke handler send ONE best-effort final delta
(finish_reason="error", usage left UNSET) on any relay.UpstreamError, right
before returning the error status — gRPC server-streaming allows sending
messages before the terminating status, so this doesn't require Relay's own
loop to change. The one gap this can't close (there's no live channel left to
send anything through): if the CALLER's own connection to provider-gateway
drops before any final delta arrives at all, there's nothing provider-gateway
can do — that case has to be handled symmetrically at the caller (data-plane's
own Step H metering: "call ended with no final delta" = incomplete, not zero).

A3 (OpenAI): real wire-format subtlety caught here, not assumed — with
stream_options.include_usage, usage arrives in a DEDICATED final chunk (empty
choices, populated usage) that comes AFTER the finish_reason chunk, not on
it. The adapter previously returned IsFinal the instant it saw finish_reason
(old design, per the file's own former header comment) — that would have
skipped the usage chunk entirely. Restructured to defer finish_reason in
stream.pendingFinishReason and keep reading one more chunk; falls back to
returning the deferred finish_reason with usage left unset if [DONE] arrives
without a usage chunk ever showing up (handles a misbehaving response
gracefully rather than losing the finish signal).

A4 (Anthropic): usage is split across TWO different events, not one dedicated
chunk — message_start carries usage.input_tokens (the very first event,
before any content), message_delta carries usage.output_tokens alongside
stop_reason. message_start previously fell into the catch-all "no data
needed" default branch and was silently discarded. Now captured into
stream.inputTokens and reunited with message_delta's output_tokens on the one
final delta. Confirmed input_tokens stays genuinely nil (not 0) if
message_start is somehow never seen.

A5 (Google): simplest of the three — Gemini's usageMetadata is a top-level
chunk field present on every chunk as a running cumulative count, just read
off the same chunk that carries finishReason. Not exercised against the real
API this phase (Dependencies.txt F13 — quota=0 on the available key); verified
against a canned real-shaped payload only, same interim measure F13 already
documents for the rest of this adapter.

A6 (provider-fake): normal/slow modes attach fixed InputTokens=42/
OutputTokens=17 to the final chunk (deterministic, for Step H4's later
metering-math tests). New fail_mid_stream mode: two real content chunks, then
a deliberately truncated JSON fragment, connection closed — simulates a real
provider breaking mid-stream, which is exactly the scenario A2's best-effort-
delta design exists for.

A7 (ProviderHealth): implemented as a pure status READ — quota.Counter gained
Peek (alongside the existing Increment) specifically so checking headroom
never itself consumes any quota; breaker.CurrentState (already built in P02
for exactly this RPC) supplies breaker state. Both fail open on a store error
at the call site (not baked into Headroom's own return value, mirroring how
Enforce already separates its fail-open Decision from its untrustworthy-on-
error current value). adapters.Registry gained Names() so "report on every
known provider" (empty ProviderHealthRequest.provider) reflects whichever
adapters actually registered, not a hardcoded list.

A8 — LIVE VERIFICATION (both services rebuilt, redeployed via onezox-stubs,
confirmed against the running cluster, not just unit tests):

1. Real usage, happy path (fake:normal): final delta carried
   inputTokens=42, outputTokens=17 — exactly matching provider-fake's fixed
   constants, confirming the full chain (fake server -> fake adapter ->
   internal Delta -> relay -> proto Delta -> grpcurl) end-to-end.

2. Real usage, mid-stream failure (fake:fail_mid_stream): FIRST ATTEMPT
   caught a real gap — the fake ADAPTER's own client-side parseMode
   allowlist didn't know "fail_mid_stream" yet (only the fake SERVER, Step
   A6, had been taught the mode), so the request was rejected client-side
   before ever reaching provider-fake. Fixed (parseMode extended), rebuilt,
   redeployed. Retried and got a SECOND, unrelated failure: DeadlineExceeded.
   Root-caused via provider-gateway's own logs to Redis Cluster connection
   timeouts ("dial tcp ... i/o timeout", "cluster has no nodes") — this
   session's host had restarted (silently, without an obvious session
   boundary) since the cluster was last known healthy, the exact
   documented WSL2-restart gossip-state-loss failure mode CLAUDE.md's
   session-start ritual exists for, hit mid-build because that ritual
   wasn't re-run after the restart. Recovered via
   scripts/recover-redis-cluster.sh (cluster_state:fail -> ok, 16384/16384
   slots) plus a manual `kubectl rollout restart deployment/provider-gateway`
   (the script only nudges dataplane-stub/provider-stub, not
   provider-gateway/provider-fake — provider-gateway's own already-running
   pods were holding a Redis client cached against pre-reset node IPs and
   needed a fresh client, not just a healthy cluster to reconnect to).
   THIRD attempt succeeded cleanly: two real content deltas delivered
   ("Hello ", "from ") — confirming already-sent content survives a later
   mid-stream failure — followed by the best-effort final delta
   (finish_reason="error", is_final=true, usage fields genuinely ABSENT,
   not zero), with the underlying RPC error correctly
   Unavailable/"decode chunk: unexpected EOF" (a real error, not a clean
   EOF that would have been misread as success).

3. ProviderHealth: empty request correctly enumerated all 4 registered
   providers (fake, openai, anthropic, google — all three real keys are
   present in this deployment); fake showed breaker=CLOSED,
   quota_headroom=0.983 (reflecting the one real request already made in
   the current window, not a stale/cached value); openai/anthropic/google
   all showed breaker=CLOSED, headroom=1.0 (correct defaults — never
   called this session, fresh window). A provider-scoped request
   correctly filtered to just that one provider's status.

LESSON RECORDED for future sessions in this environment: a silent host
restart (no explicit "new session" signal, just a date rollover noticed
mid-task) can leave Redis Cluster gossip state stale WITHOUT the working
directory or conversation otherwise looking any different — the failure
only surfaced as a mid-build timeout on an otherwise-unrelated test. The
session-start ritual (recover-redis-cluster.sh + confirm cluster_state:ok)
needs to be treated as "run when in doubt," not only literally at the
first message of a conversation.
--------------------------------------------------------------------------------
2026-08-04 — Step B: data-plane skeleton deployed; live verification found
provider-gateway-mtls didn't recognize the new caller identity
--------------------------------------------------------------------------------
Skeleton built: DataplaneService.Submit as a stub (no classifier/DAG/
scheduler — Steps C/D/E), completing the RPC with a clearly-marked
placeholder response rather than a bare UNIMPLEMENTED status, matching
dataplane-stub's own established "stub that completes successfully with
canned content" idiom. Structured logging on the SUCCESS path built in
from the start (both "request received" and "request completed" log
lines) — the exact gap Phase-01 (edge-gateway Step J3) and Phase-02
(provider-gateway Step N3) both had to retrofit.

FINDING: first deploy crashlooped on startup. Boot sequence connects to
CockroachDB (health_probe insert) and Redis (set/get) successfully, then
crashed calling provider-gateway's ProviderHealth RPC — AioRpcError
UNAVAILABLE, "failed to connect to all addresses". Root cause:
provider-gateway-mtls's ingress rule for port 50051 only recognized
app=dataplane-stub as an allowed caller — data-plane's own real Cilium
identity wasn't in the allow-list, so the connection was correctly
blocked by the mesh, not a code bug. This is exactly the relationship
that policy's own comment already anticipated ("Phase-02.txt's own real
caller relationship, data plane -> provider gateway, doesn't arrive
until Phase-03") — Phase-03 had now arrived.

FIX: widened provider-gateway-mtls's port-50051 ingress rule to allow
EITHER dataplane-stub OR data-plane (matchExpressions, In) — a widening,
not a replacement; dataplane-stub keeps its access until Step J's
cutover removes it, same mesh-identity-safe sequencing used throughout
this project (never remove the old identity's access before the new
one is confirmed healthy). Updated the file's stale header/rule comments
to match, rather than leaving them describing a "doesn't arrive until
Phase-03" state that was no longer true.

VERIFIED after the fix: data-plane reached 1/1 Running, full boot
sequence completed (health_probe row written, Redis round-trip
confirmed, provider-gateway ProviderHealth returned all 4 registered
providers with real breaker/quota state). A live Submit call via grpcurl
returned the placeholder response correctly, and BOTH the entry
("request received") and completion ("request completed") structured
log lines appeared — confirming the success-path logging discipline is
genuinely built in, not just present in the code and never exercised.

Preliminary deployment only — no CiliumNetworkPolicy of data-plane's own
yet, no cutover, dataplane-stub still the active service edge-gateway
forwards to. Both arrive at Step J.
--------------------------------------------------------------------------------
2026-08-04 — Step E: scheduler.place proven to genuinely react to live
provider health, all four signal states
--------------------------------------------------------------------------------
The point of this step was never "call ProviderHealth" — it was proving
bind_via_provider_health's decision actually CHANGES based on what that
call returns. A binder that ignores the response looks identical to a
working one exactly as long as the provider stays healthy; the 22 unit
tests committed alongside this step prove the pure decision logic
(bind()) is correct for every input combination, but only a live test
against the real deployed provider-gateway proves the WIRING between the
real RPC response and that decision logic is genuine, not just plausible
in isolation.

METHOD: a standalone script (not wired into Submit — Step E is logic +
live proof only, matching Steps C/D's "no premature wiring" discipline)
imported scheduler.place directly and called bind_via_provider_health
against a port-forwarded, already-deployed provider-gateway, driving the
real "fake" provider's breaker/quota Redis state into each condition
before each call.

1. HEALTHY (breaker closed, full headroom — the state already present):
   BOUND worker_ref='fake:normal'.
2. EXHAUSTED (breaker OPEN): tripped for real via 5 consecutive
   worker_ref=fake:fail Invoke calls through provider-gateway (confirmed
   via a live FALLBACK_REASON_BREAKER_OPEN on the 5th, then via a direct
   Redis read of provider:fake:breaker showing state=open) ->
   REFUSED provider='fake' reason='circuit breaker is open'.
3. DEGRADED (breaker HALF_OPEN): backdated provider:fake:breaker's own
   opened_at past BREAKER_OPEN_DURATION_SECONDS so provider-gateway's
   own effectiveState logic derives half-open — confirmed via a live
   ProviderHealth call showing BREAKER_STATE_HALF_OPEN BEFORE testing
   the binder, so the test is known to be exercising what it claims to
   -> BOUND worker_ref='fake:normal' (the case that actually proves the
   reaction isn't a blunt "refuse anything imperfect": half-open must
   still bind, letting the trial call through, or provider-gateway's own
   breaker could never recover — no trial call would ever reach it).
4. EXHAUSTED (quota): set provider:fake:quota:{current_window_bucket} to
   60 (the default QUOTA_LIMIT_PER_MINUTE). First attempt landed in a
   bucket that expired between the SET and the check (a genuine 60s
   window-boundary race, caught by ProviderHealth's own reported
   headroom, not assumed) — redone against the then-current bucket,
   confirmed via ProviderHealth (quotaHeadroom absent from the JSON,
   which for a proto3 scalar float at exactly 0.0 means zero, not a
   missing/broken field — proto3 JSON omits default-valued fields) ->
   REFUSED provider='fake' reason='fleet-wide quota exhausted'.

All test-induced state reset afterward (breaker forced back to
{"state":"closed","failures":0,"opened_at":0}, the quota key deleted) so
later steps start from a clean "fake" provider, not test debris.
--------------------------------------------------------------------------------
2026-08-04 — Step F: both statelessness halves proven under a real,
hard mid-request pod kill; Dockerfile found to be silently stale
--------------------------------------------------------------------------------
FINDING (caught rebuilding for this test, before any live verification
ran): data-plane's Dockerfile still only copied main.py and generated/ —
written in Step B, before planner/, scheduler/, working_memory/, or
request_log/ existed, and never updated as each was added across Steps
C-F. The rebuilt image's manifest hash came back byte-identical to the
Step B build, which is what surfaced it — none of these modules have
been wired into main.py/Submit yet, so the running service never
actually needed to import them, and this would otherwise have surfaced
as a confusing failure the moment something finally did. Fixed by adding
the four missing COPY lines; confirmed the rebuilt manifest hash actually
changed this time.

METHOD (both halves must pass, testing one is a false pass): from inside
a live, already-deployed data-plane pod (not from the host — Redis
Cluster's multi-shard topology makes host-side port-forward unreliable
for a cluster-aware client, the same reason provider-gateway and
data-plane both got preliminary in-cluster deployments), ran the REAL
working_memory.write()/request_log.write() functions (not a hand-rolled
simulation) to write: a working_memory key (ttl=30s, simulating
"currently in-flight" state) and a request_log row (simulating "a prior,
already-completed request's committed row"). Confirmed both readable.
Then hard-killed that EXACT pod (`kubectl delete pod --grace-period=0
--force` — no graceful shutdown window, matching the "unhappy path, not
clean completion" discipline Phase-01's H3 established) and checked
state at three points, not two, to make the causality unambiguous:

1. Immediately after the write (before the kill): both present.
2. ~1 SECOND after the hard kill (TTL showing 29 of 30 remaining):
   working memory key STILL FULLY PRESENT — proves Redis has zero
   awareness of or dependency on the pod's liveness; the kill itself
   does not and cannot trigger deletion. request_log row also present,
   unaffected (CockroachDB isn't running in the pod that died). A first
   attempt at this exact check used an 8s TTL and the sequential tool
   calls (write, then a separate kill command, then a separate check
   command) took long enough in wall-clock time that the TTL had
   already elapsed naturally by the time of the check — an ambiguous
   result, redone with a 30s TTL specifically to leave an unambiguous
   margin rather than trusting a result that could have been explained
   two different ways.
3. 32 SECONDS after the kill (past the 30s TTL): working memory key
   GONE (TTL command returns -2, "key does not exist" — not 0, not an
   error). request_log row STILL PRESENT, completely unaffected by the
   TTL, which was never a property of that table at all.

A fresh data-plane pod also came up on its own (Deployment's normal
reconciliation) and reached 1/1 Ready without intervention — the service
itself recovers cleanly from a hard kill, not just the data.

Both halves of the boundary this phase's statelessness claim is actually
about are now proven, not assumed: ephemeral state evaporates (via a TTL
that was directly confirmed to be SET, not just intended — the
immediate-after-kill check literally read the countdown), and durable
state persists elsewhere, entirely independent of any specific
data-plane pod's fate.
--------------------------------------------------------------------------------
2026-08-04 — Step G: assembled Submit flow verified live against
provider-fake; caught a stale Redis Cluster (session-start ritual
skipped) and confirmed model source is a deployment-level static ref,
not a per-request override
--------------------------------------------------------------------------------
FINDING 1 (pre-existing, not introduced by Step G): after syncing the
Step G image, both the new and the pre-existing data-plane pods hung
forever right after "wrote health_probe row" and never opened port 8080,
so the liveness probe killed them (exit 137) in an endless CrashLoop.
`redis-cli cluster info` showed cluster_state:fail (5461/16384 slots ok,
10923 pfail) — the WSL2 host-restart gossip-state-drift failure class
CLAUDE.md's session-start ritual exists specifically to catch, and this
session had not run scripts/recover-redis-cluster.sh before starting.
The pod wasn't crashing on a bug; it was blocked inside its own Redis
Cluster client setup, indefinitely, with no timeout — a silent hang, not
a loud failure, which is worth remembering: a stuck-not-crashing pod
with no further log output after a known I/O step is now a first-line
Redis Cluster health suspect. Ran the recovery script (reset the 6
redis-cluster pods, re-ran redis-cluster-init, confirmed 16384/16384
slots ok); it also has a step 6 that nudges any stuck Redis-dependent
stub pods, which threw "provider-stub: no pods found" — a stale
reference to a pod pruned in Phase-02, not a new problem. Deleted the
stuck data-plane pods to force a clean reschedule against the
now-healthy cluster; the fresh pod booted cleanly through every stage,
including the Step G smoke-test line ("boot: all required modules
present: [...aggregator]").

FINDING 2: the Step G task description asked to test fake:slow/fake:fail
via a per-request `worker_ref`, but Phase-03's scheduler binds against
STATIC_MODEL_REF (a Deployment-level env var), and Submit's request.model
field is not consulted — this is the phase's own documented constraint
("Model source is a STATIC ref this phase... do NOT build registry
lookup now"), not an oversight. A grpcurl call with model:"fake:slow"
therefore silently ran against whatever STATIC_MODEL_REF actually was
(fake:normal), returning in 0.07s instead of the expected ~4.2s —
caught by checking timing, not by an error, which is exactly the kind of
false-positive Step C/D's "assert the negative" discipline warns about.
First fix attempt was `kubectl set env deployment/data-plane
STATIC_MODEL_REF=fake:slow` directly against the cluster; Argo CD's
self-heal reverted it within the sync interval (confirmed via `kubectl
get deployment ... -o jsonpath='{...env}'` flipping back to fake:normal
on its own) — a correct enforcement of CLAUDE.md's "never kubectl apply
directly, go through Argo CD" rule, not a bug, and a useful live
confirmation that self-heal is actually wired up. Redone properly: three
sequential git-tracked commits (fake:slow -> fake:fail -> fake:normal),
each pushed and `argocd app sync onezox-stubs`'d before testing, ending
on fake:normal as the lasting state.

METHOD/RESULTS (live, in-cluster, via grpcurl against the pod's Service,
port-forwarded — host-side gRPC works fine here, unlike Redis Cluster's
client-side MOVED-redirect problem):
  - fake:normal: full assembled chain (admit -> classify -> template ->
    working_memory write -> scheduler bind -> aggregator relay ->
    working_memory delete) produced the correct 6-chunk stream plus a
    final is_final=true/finish_reason="stop" message, and both the
    "request received" and "request completed
    latency_ms=..." structured log lines appeared — the
    integration proof AND, since this is the first Submit call to
    actually execute classifier/templates/scheduler/working_memory/
    aggregator inside the running pod, the image-completeness proof
    Step F's Dockerfile bug motivated adding verify_modules_present()
    for.
  - fake:slow: 6 chunks at 700ms apiece streamed through the full chain
    (provider-fake -> provider-gateway -> data-plane -> grpcurl client)
    with measured wall-clock elapsed=4.28s, matching 6x700ms — confirms
    no buffering/batching anywhere in the relay path collapses the
    delay, i.e. backpressure genuinely holds end-to-end rather than the
    provider-gateway or data-plane draining the upstream stream early
    into memory.
  - fake:fail: provider-fake returns HTTP 503 with no streaming at all;
    provider-gateway's adapter (Step A2's "error-path best-effort
    delta") surfaces one best-effort Delta with finish_reason="error"
    before the stream terminates with a genuine grpc.RpcError
    (UNAVAILABLE); aggregator.relay() yields that delta normally, then
    the next iteration raises ProviderStreamError, which Submit converts
    to a clean `context.abort(UNAVAILABLE, ...)` — the client-visible
    result is one partial message followed by a single well-formed
    UNAVAILABLE status (grpcurl's own "ERROR: Code: Unavailable" block),
    not a raw transport failure or a hang. The pod's own "provider
    stream error ..." WARNING log line appeared. Confirmed the `finally:
    await admit.release(...)` held on this error path too: read
    `admission:data-plane:inflight` from Redis directly after the call
    and got 0, not a leaked count.

Both of Step G's Dockerfile-completeness and backpressure/error-path
claims are now proven live, not just unit-tested; the Redis Cluster
incident is the fourth time this exact failure class has surfaced this
build (see A1-A8's entry) and reinforces that the session-start ritual
is not optional even mid-phase.
--------------------------------------------------------------------------------
2026-08-04 — Step H: real metering (usage_event/request_log) — EC2 gate
verified live: real tokens, three-way reconciliation, honest incomplete
usage, and correct row presence across both dispatch and pre-dispatch
failure paths
--------------------------------------------------------------------------------
Migrations were already applied (Step F pulled 0006/0007 forward for its
own pod-kill proof) — reconfirmed both tables still exist with the right
shape, no new migration needed. Step H's actual job, per that migration's
own comment, was deciding exactly when usage_event gets written with
fields NULL vs populated.

SCOPE DECISION, stated plainly rather than assumed silently: usd_cost
stays NULL this phase. No pricing/rate table exists anywhere in this
codebase (grepped for cost_per_token/price_per/usd_cost — the only hits
are the column itself and edge-gateway's own PLACEHOLDER_USD_COST=0.0
from Phase-01, which data-plane's real numbers don't need to match since
dataplane.proto's SubmitResponse never gained token/cost fields — Part
L's metering lives on data-plane's own span + usage_event, not the wire
contract edge relays to the client). Per-model pricing belongs to the
signed model registry (Phase-04, Dependencies.txt F10); writing a
fabricated rate now would be the same "looks right, isn't" mistake this
step exists to rule out for tokens, just applied to cost instead.

DESIGN: aggregator.relay() now yields (SubmitResponse, Usage | None)
pairs — Usage captured exactly once, off the final delta only (provider.
proto's own "set ONLY on the final delta" convention), fields
individually None when the provider didn't report them. main.py's new
_finish_request() helper writes request_log on EVERY exit path (six of
them: admission_shed, unsupported, provider_unavailable,
provider_fallback, provider_stream_error, ok) but usage_event only when
model_ref is not None — the function's own signal that a provider
dispatch was actually attempted. Four of the six failure/success paths
never reach a provider at all (admission shed, classifier reject,
ProviderUnavailable from the pre-dispatch health check, and
provider-gateway's own pre-dispatch FallbackSignal, per relay.go: the
breaker/quota check that raises FallbackError happens BEFORE the
adapter's Invoke is ever called) — these get model_ref=None, no
usage_event row at all, deliberately, not even an all-NULL one:
manufacturing a billing row for a call that provably never touched a
provider would falsely imply "maybe tokens were spent" about something
that structurally could not have. Only provider_stream_error (a genuine
mid-dispatch break) and ok get a usage_event row. Span attributes
(tokens_in/tokens_out/orch_tokens/model_ref/status) are set from the
exact same local variables as the usage_event write, in the same
function call — cannot independently drift by construction, which is
what the "span matches usage_event" proof below is actually checking.

UNIT TESTS (the "math," against provider-fake's REAL canned numbers, not
arbitrary stand-ins — 42/17, read directly from services/provider-fake/
main.go's cannedInputTokens/cannedOutputTokens so a future drift there
is caught): relay() captures Usage(42, 17) exactly on the final delta
and None on every delta before it; relay() captures Usage(None, None) —
verified != Usage(0, 0) — on provider-gateway's best-effort synthetic
error delta; relay() never reads token fields off a non-final delta even
if hypothetically present. usage_event.write() passes None through to
the SQL parameter as None, not coerced to 0. 46 tests total, full suite
and each file individually.

LIVE VERIFICATION (in-cluster, via grpcurl against the deployed pod,
plus a direct CockroachDB query and a Tempo trace query):

1. REAL TOKENS, confirmed not estimated: a fake:normal Submit wrote
   usage_event with tokens_in=42, tokens_out=17 — byte-identical to
   provider-fake's own documented canned constants, not a char-count
   estimate arrived at independently.

2. THREE-WAY RECONCILIATION (the EC2 proof): for that same request,
   queried Tempo directly (`GET /api/search?tags=request_id=...` ->
   `GET /api/traces/{traceID}`) and read the data_plane.submit span's
   own attributes: tokens_in=42, tokens_out=17, orch_tokens=0,
   model_ref="fake:normal", status="ok" — identical, field for field, to
   both the usage_event row and provider-fake's real reported numbers.
   All three independent records of the one event agree.

3. INCOMPLETE-USAGE PATH: switched STATIC_MODEL_REF to
   fake:fail_mid_stream (genuinely mid-stream: two real content chunks
   delivered, "Hello " / "from ", before the connection breaks — the
   sharper proof than immediate-fail, which never streams any content at
   all) via the same git-tracked-commit/Argo-sync/revert discipline Step
   G established (kubectl set env gets reverted by Argo's self-heal, on
   purpose). Result: usage_event wrote tokens_in=NULL, tokens_out=NULL,
   orch_tokens=0 — NOT zero tokens_in/out pretending the call was free.
   request_log recorded status="provider_stream_error". Client received
   the two real chunks, then a clean UNAVAILABLE gRPC status — no hang,
   no raw transport error leaking through.

4. BOTH ROWS, BOTH PATHS — verified the full matrix, not just the two
   headline cases: ok (both rows, real numbers), provider_stream_error
   (both rows, honestly incomplete) — and, to prove the "no usage_event
   for pre-dispatch failures" design decision is actually true and not
   just asserted, sent a REQUEST_KIND_EMBEDDINGS request (classifier
   rejection, no provider ever contacted): request_log got a row
   (status="unsupported"), usage_event got ZERO rows for that
   request_id, confirmed by direct count query. This is the sharper
   proof of "honest, not manufactured" — a blanket "always write both"
   rule would have hidden this distinction entirely.

Restored STATIC_MODEL_REF to fake:normal (git-tracked, Argo-synced) as
the lasting state after all three temporary routing changes.
--------------------------------------------------------------------------------
2026-08-04 — Step I: tenant isolation (EC3), positive-controlled and
adversarial — same discipline as P01's H2 and P02's L1/L2
--------------------------------------------------------------------------------
SCOPE DECISION, asked and confirmed rather than guessed: the
identity-reject path validates ONLY org_id, not user_id/project_id, even
though Phase-03.txt's Part O text reads "org_id/user_id/project_id...
no call proceeds without it." Checked edge-gateway's own normalize.rs
first: `org_id: identity.org_id.to_string()` (never Option) versus
`user_id: identity.user_id.map(...).unwrap_or_default()` and the same
for project_id — edge-gateway's own real design treats user_id/project_id
as legitimately absent (e.g. API-key-only auth resolving no specific
user/project), sent as "" on entirely normal traffic. Requiring them
non-empty here would reject real, legitimate requests edge-gateway is
designed to send; org_id is the only field every downstream FK
(usage_event.org_id, request_log.org_id) actually depends on. Asked the
user directly given the tension between the architecture text and the
real code; confirmed org_id-only.

THREE PROOFS, all live and in-cluster:

1. IDENTITY-REJECT PATH: a Submit with identity.org_id="" got a clean
   InvalidArgument ("request missing required org_id"), immediately, via
   identity.validate() checked before admission or anything else runs.
   Confirmed zero usage_event AND zero request_log rows for that
   request_id — no row under a placeholder/default org_id either, which
   would itself have been a cross-tenant leak by omission. A structured
   WARNING log line is the only record (there is no real tenant to
   attribute a durable audit row to).

2. POSITIVE CONTROL, done BEFORE trusting the concurrent test (same
   order P01's H1 and P02's L1 used — prove the detector can catch a
   planted problem before trusting a clean result from it): manually
   INSERTed a usage_event row for request_id='positive-control-leak-test'
   tagged with Tenant B's org_id, then ran the exact comparison the real
   test would use ("does this request's org_id match its expected
   tenant?") — it correctly returned false. Deleted the planted row
   before proceeding. This is what makes the concurrent test's "all
   matched" result below trustworthy rather than a rubber stamp.

3. BEHAVIORAL, UNDER REAL CONCURRENCY (not structural — different Redis
   keys prove nothing on their own): two real tenants (org_ids
   044122c4-.../16b33bc8-...) driven through genuine concurrent Submit
   calls, both against fake:slow specifically so they overlap in actual
   wall-clock time (6x700ms chunks each, ~4.2s), each carrying a unique
   canary message ("CANARY-TENANT-A-f3a9c1" / "CANARY-TENANT-B-9d7e42").
   Fired both in the background of one shell, slept 1.5s, then read
   BOTH workingmem: Redis keys while BOTH requests were still genuinely
   in flight simultaneously (confirmed by the sleep landing mid-stream,
   well before either's ~4.2s completion) — each key held ONLY its own
   tenant's canary, zero cross-contamination. Grepped both full response
   streams for the other tenant's canary string: zero matches either
   direction (moot for provider-fake's own canned content, which doesn't
   echo the prompt, but confirmed anyway as the user-facing consequence
   check). After both completed: usage_event/request_log rows for both
   request_ids carried the correct, non-swapped org_id (re-using
   Finding 2's exact comparison, now trusted) and both workingmem: keys
   were gone (the Step F cleanup proof, now reconfirmed under genuine
   concurrent load rather than a single sequential request).

HONEST LIMITATION, worth stating plainly rather than leaving implicit:
Redis working-memory isolation in this phase is NOT enforced by any
per-tenant ACL (Redis Cluster has none configured, and building one is
out of scope) — it holds because (a) request_id is a server-generated,
unguessable UUIDv4 (edge-gateway's pipeline.rs: `Uuid::new_v4()`, never
client-supplied) and (b) data-plane's own code always reads/writes
workingmem: using the exact per-request local `request_id` variable,
never a shared or global one. The concurrency proof above is what
actually establishes (b) is true in the running code, not merely
asserted from reading it.

TRACKER NOTE for the Phase-03 completion record: usd_cost stays NULL for
the whole phase (Step H's own decision, re-confirmed here) — no
pricing/rate source exists anywhere in this codebase; that belongs to
the model registry (Phase-04, Dependencies.txt F10). Carry this forward
as an explicit, named deferral in Phase-03-Complete.txt, not a silent
gap.
--------------------------------------------------------------------------------
2026-08-04 — Step J: mesh-identity-safe cutover from dataplane-stub —
the real slice is now live end-to-end, a genuine EC2-breaking bug was
caught and fixed by this step's own "verify against the real slice"
requirement, and a process mistake (an uncommitted "commit") was caught
by Argo CD's own refusal to drift
--------------------------------------------------------------------------------
SEQUENCE, same mesh-identity-safe ordering as P01 Step F2 / P02 Step K3:
(1) data-plane-mtls added and confirmed VALID:true, both identities
(edge-gateway=889, data-plane=32903) confirmed resolved via `cilium-dbg
identity list` — BEFORE (2) the switch (edge-gateway's
DATAPLANE_STUB_ENDPOINT default flipped from dataplane-stub to
data-plane, both the code default AND the deployed image, so a fresh
clone with no env override also reflects reality, not just a manifest
override layered on a stale default) — BEFORE (3) dataplane-stub's own
removal. No window where the real data plane was live without mesh
identity.

FINDING 1 (real, EC2-breaking bug, caught only because this step verifies
against the real slice, not grpcurl): the first real request through
edge-gateway's actual public endpoint got a complete, correct SSE
response — but data-plane's own logs showed only "request received",
never "request completed", and usage_event/request_log got ZERO rows.
Root cause: main.py's Submit handler yielded every response inline,
including the final one, then ran working_memory.delete()/
_finish_request() only once the `async for` loop over aggregator.relay()
exited — which only happens once the CLIENT polls the gRPC stream one
more time past the final chunk. grpcurl happens to drain to the true
stream end (which is exactly why Steps G/H/I's own live verification,
all done directly via grpcurl, never surfaced this). edge-gateway's own
stream.rs is explicit about its behavior: "stops at the first is_final
chunk" — entirely reasonable client behavior — so every real request
silently never got metered. Confirmed via Redis: admission counter was
back to 0 and the workingmem: key was already gone (working_memory.
delete() DID run), but no DB rows existed — meaning the generator ran
past cleanup's first line and was cut off specifically inside
_finish_request() itself, consistent with a client-driven stream
cancellation landing mid-write. FIXED by holding the final response back
and running working_memory cleanup + metering BEFORE yielding it, so the
server's own bookkeeping never depends on the client polling again after
it has everything it needs — re-verified with a second real request
through edge-gateway: "request completed" logged, usage_event/
request_log both correct (tokens_in=42, tokens_out=17, org_id correct).

FINDING 2 (process mistake, self-caught): after the first cleanup commit
("prune dataplane-stub..."), Argo CD kept reporting dataplane-stub-mtls
as still Synced/tracked no matter how many times `argocd app sync
--prune` and `--hard-refresh` were run. Root cause: `git rm -r
services/dataplane-stub platform/stubs/dataplane-stub` auto-stages
deletions, but the follow-up `git add <specific files>` for the actual
EDITS (ciliumnetworkpolicy.yaml, ci.yml, prometheus-values.yaml,
ingress.rs) silently failed to stage them — the commit that claimed to
remove dataplane-stub-mtls only ever contained file deletions, no
insertions, confirmed by `git show <commit> -- ciliumnetworkpolicy.yaml`
returning an empty diff. Argo CD was correctly refusing to prune a
resource still present in git — it caught a genuine gap between "commit
message says X" and "commit actually contains X" that a less careful
sync tool might have silently gone along with. Fixed with a follow-up
commit that actually staged and committed the edits, then a fresh
rebuild/retag of edge-gateway (same image digest as before — the source
was already on disk when the earlier image was built, just not
committed to git — but the tag now correctly traces to a commit that
contains it, restoring the "image tag = git commit SHA" contract).

CLEANUP, to the P02 completeness bar: services/dataplane-stub/ and
platform/stubs/dataplane-stub/ deleted outright; dataplane-stub-mtls
removed (confirmed `Pruned` by Argo, not just absent from the file);
the dataplane-stub -> edge-gateway reverse rule removed from
edge-gateway-mtls (a pure Phase-00 capability-proof pairing, never
functionally used, matching the precedent provider-gateway-mtls's own
header comment already set for the equivalent reverse rule at P02 K3);
provider-gateway-mtls's port-50051 rule narrowed from the Step B
coexistence matchExpressions [dataplane-stub, data-plane] back to a
single data-plane matchLabels rule (confirmed via a fresh `kubectl get
-o yaml`, not assumed from the diff); CI matrix and Prometheus scrape
target both swapped to data-plane; edge-gateway's two user-facing
"dataplane-stub" strings (a bad_gateway error message, a warn! log line)
updated to "data-plane" for accuracy.

FINAL LIVE VERIFICATION (after the real fix AND the real cleanup were
both actually deployed — not the false-clean intermediate state):
authenticated request through edge-gateway's real /v1/chat/completions
(seeded a dedicated phase03-step-j-test-tenant via the established
data/seed/seed-test-tenant.sh rather than touching Phase-01's shared
test tenant/key) produced a complete SSE stream; data-plane's own logs
showed "request completed"; usage_event/request_log both correct
(tokens_in=42, tokens_out=17, correct org_id, status=ok); a fresh
`cilium-dbg bpf auth list` (queried against the CORRECT node's Cilium
agent — the first attempt queried the wrong node and found nothing,
since edge-gateway and data-plane both schedule onto onezox-dev-worker,
not the node the cilium pod picked by pod-list-order happened to be on)
showed a freshly-timestamped spire-authenticated pairing between
identity 889 (edge-gateway) and 32903 (data-plane). The slice is now
edge-gateway -> data-plane -> provider-gateway -> provider-fake, for
real, with dataplane-stub gone.

TRACKER NOTE, carried forward again for the Phase-03 completion record:
usd_cost stays NULL (Step H/I) — no pricing source exists yet, belongs
to Phase-04's model registry. Redis working-memory isolation relies on
unguessable server-generated request_ids and correct per-request
scoping in code, not a per-tenant ACL (Step I) — both still true and
unaffected by this step.
--------------------------------------------------------------------------------
2026-08-04 — Step K: the one real call (EC1) — clean success, no
manufactured failure
--------------------------------------------------------------------------------
CORRECTION to this record's own process: the user's Step L instruction
initially described Step K as already complete, with a specific
"failed, diagnosed, fixed, succeeded" narrative — that description was
the user's own error (retracted immediately when asked), not something
that had actually happened. Checked first (grepped this file for any
Step K entry, found none) rather than assuming or fabricating a result,
then confirmed directly with the user before proceeding. Recorded here
is what ACTUALLY happened, live, in this session.

Prerequisites already in place from Phase-02: real ANTHROPIC_API_KEY
mounted in provider-gateway (Step J3, envFrom secretRef), api.anthropic.com
already egress-allowed (Steps J4-J6), model ID
claude-haiku-4-5-20251001 (Anthropic's cheapest current tier) already
data-plane's own STATIC_MODEL_REF fallback default. Flipped the
deployed override from fake:normal to the real ref (git-tracked, Argo-
synced, same discipline as every fake:slow/fake:fail test all phase).

RESULT: the real call succeeded on the FIRST attempt — a clean result,
recorded as clean, not manufactured into a failure to match a prior
(incorrect) framing. Seeded a dedicated phase03-step-j-test-tenant
(data/seed/seed-test-tenant.sh) rather than reusing Phase-01's shared
test tenant or its unrecoverable raw key. Full streamed response through
edge-gateway's real public endpoint; usage_event wrote tokens_in=25,
tokens_out=15 — Anthropic's own REAL numbers, visibly different from the
fake's canned 42/17, confirming this genuinely reached the real API, not
an accidentally-still-fake path. request_log status=ok. Anthropic's own
native finish_reason ("end_turn") passed through as-is, distinct from
the fake's OpenAI-style "stop" — a genuine, expected difference in real
provider behavior the fake never has occasion to exercise.

EC1 is proven: a user with a valid API key receives a streamed
frontier-model answer end-to-end (edge -> data plane -> provider-gateway
-> real Anthropic -> SSE).

--------------------------------------------------------------------------------
2026-08-04 — Step L: telemetry + stability (EC4), the last exit
criterion — a real, previously-undetected gap spanning all three
services and every prior phase, caught only by this step's own
requirement to count trace IDs, not just confirm spans export
--------------------------------------------------------------------------------
FINDING (the headline one): querying Tempo for Step K's own real-call
trace returned THREE separate trace IDs — one rooted at edge-gateway,
one at data-plane, one at provider-gateway — not one connected tree.
Each service's spans were individually well-formed and genuinely
exported; that is exactly why this was never caught before. P01's own
J1 fixed edge-gateway's span tree WITHIN that one service; P02's N1-N3
did the same for provider-gateway; nobody had ever verified a trace
survives crossing a service boundary, because this is the first phase
where three real services actually chain together end-to-end.

ROOT CAUSE, identical in shape across all three languages: NONE of
edge-gateway (Rust/opentelemetry), data-plane (Python/opentelemetry), or
provider-gateway (Go/otel) had ever called set_text_map_propagator.
Every OTel SDK except Python's defaults to a no-op propagator — so even
if inject()/extract() call sites had existed (they didn't, anywhere),
they would have silently done nothing. Confirmed this precisely before
writing any fix: grepped each service's dependency manifest for a
gRPC-context-propagation package (none), then confirmed Python's
opentelemetry.propagate.get_global_textmap() genuinely returns a real
CompositePropagator by default (verified interactively, not assumed) —
the one asymmetry across the three SDKs that shaped where explicit
propagator setup was actually needed.

FIX, one hop at a time, all three services in one coordinated commit
(the fix only works once every side of every hop has it):
  - edge-gateway: opentelemetry::global::set_text_map_propagator(...)
    added to init_telemetry(); dataplane_client::submit() now injects
    the current span's context into the outgoing tonic Request's
    metadata via a small MetadataMap Injector adapter.
  - data-plane: Submit() now extracts incoming trace context from
    context.invocation_metadata() BEFORE starting its own root span
    (previously always parentless, even though edge-gateway's call
    always carried the real parent by the time this ran); scheduler.
    place.bind_via_provider_health and aggregator.relay both inject the
    active span's context into their own outgoing calls to
    provider-gateway.
  - provider-gateway: otel.SetTextMapPropagator(propagation.TraceContext{})
    added; both Invoke and ProviderHealth now extract incoming context
    via a shared helper. ProviderHealth had NO span of its own at all
    before this — scheduler.place's own health-check call was invisible
    in the tree regardless of propagation; fixed alongside the
    propagation fix, not left for later.

LIVE VERIFICATION, all three points, against a SECOND real Anthropic
call — deliberately not reused from Step K's own trace, because a
BROKEN trace can't prove a FIX; verifying the fix genuinely required a
new minimal real call, made and recorded honestly as a second real call,
not folded silently into Step K's one-call framing:

1. SPAN TREE: queried Tempo for the new request_id — ONE trace ID now,
   not three. Fetched the full trace and parsed parent-child links
   directly (not just trusting a shared trace ID, which the search API
   could in principle group without genuine nesting): 15 spans across
   edge-gateway/data-plane/provider-gateway. edge_gateway.request is the
   true root; data_plane.submit's parent is an edge-gateway span
   (edge_gateway.submit); provider_gateway.invoke AND
   provider_gateway.provider_health both parent under data_plane.submit;
   provider-gateway's own internal children (coalesce/quota/breaker/
   adapter/stream) nest correctly under provider_gateway.invoke. One
   connected tree, confirmed by actual parent span IDs, not inferred.

2. DATA-PLANE SUCCESS-PATH LOG: present, structured, JSON, request_id/
   org_id/latency_ms only. Leak check re-run with P01-H1/P02-L1's exact
   positive-controlled discipline: first confirmed the grep/Loki query
   COULD find something genuinely present (request_id — found via
   kubectl logs immediately; via Loki, the FIRST attempt returned zero
   results with a too-narrow time window, itself almost a false "clean"
   pass — caught by not trusting an absent result until the same query
   found something real with a wider window). Then searched
   data-plane + provider-gateway (all 3 replicas) + edge-gateway, both
   kubectl logs and durable Loki, for the raw Anthropic key (bare and
   "Bearer <key>" form) and the real message content: zero matches,
   everywhere, both stores.

3. STABILITY, sustained load, local-kind framing: 20 concurrent workers
   against data-plane directly, fake:slow (~4.2s/request, needed for
   genuine sustained in-flight overlap, not requests completing before
   the next sample), 60 real wall-clock seconds, sampling
   workingmem:* key count every 5s across all 3 Redis Cluster master
   shards (counting only masters, not their mirroring replicas, to
   avoid double-counting). 280 total requests completed (20 workers x
   14 each). Key count stayed BOUNDED at ~20 throughout the entire load
   window (one sample briefly read 25 — consistent with request
   overlap at a sampling instant, not accumulation: no upward drift
   across 10 samples spanning 50s) — proving the drain rate keeps pace
   with the creation rate at volume, not just for Step F's one request.
   Within 5s of the workers stopping, the count dropped to 0 and stayed
   at 0 for every remaining sample (30s+ of confirmed post-load
   stability) — TTL/explicit-delete both hold under real concurrent
   load, not just in isolation.

Restored STATIC_MODEL_REF to fake:normal (git-tracked, Argo-synced) as
the lasting state after both Step K's and Step L's real-call work.

TRACKER NOTE, carried forward once more for the Phase-03 completion
record: usd_cost stays NULL (Steps H/I) — belongs to Phase-04's model
registry. Redis working-memory isolation relies on unguessable
server-generated request_ids and correct per-request scoping, not a
per-tenant ACL (Step I) — now also reconfirmed bounded/draining
correctly under real concurrent load (Step L), not just isolated.
================================================================================
END OF PHASE-03-PROGRESS.TXT (living document — append, do not rewrite)
================================================================================
