[workspace]
resolver = "2"

# services/edge-stub joined this phase (Phase-00 Deployment Step 7).
# Still to come (Part C, Part Q):
#   - services/edge-gateway     (later: real public ingress / streaming relay)
#   - services/sandbox-supervisor (later: Firecracker microVM supervisor)
#   - libs/tokenizer            (later: Rust/PyO3 extension for the Python data-plane)
members = ["services/edge-stub"]

[workspace.package]
edition = "2024"
