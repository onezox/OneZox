// Package policy implements per-org policy rule reads/writes, backed by
// the policy table (data/migrations/0010). Implementation lands in
// Phase-04 Step H, its own commit — this file exists only so the package
// compiles as part of control-plane's Step D skeleton.
package policy
