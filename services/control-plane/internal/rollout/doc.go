// Package rollout is scaffolded only, per Phase-04.txt's own scope line:
// "rollout/ (rollout controller wiring; canary UX in Phase-05)". Present
// and typed so the module boundary Phase-04.txt specifies exists;
// deliberately NOT implemented in Phase-04 — the canary controller and
// Argo Rollouts wiring are Phase-05 scope (CLAUDE.md's Phase-04 section:
// "Admin panel + Argo Rollouts canary are Phase-05 — do NOT build the
// rollout UX or canary controller here"). Do not add real logic to this
// package during Phase-04.
package rollout
